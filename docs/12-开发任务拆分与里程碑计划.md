# AI 审美形成系统：开发任务拆分与里程碑计划

## 1. 文档职责

这份文档只负责把当前版本拆成可执行开发任务。

它不重新定义版本路线，也不保存历史完成状态：

- 版本目标和版本边界以 `02-版本迭代路线图.md` 为准。
- MVP 产品范围以 `03-MVP功能需求文档.md` 为准。
- 已归档版本的工程验收清单以 `archive/<version>/` 为准。
- 根目录 `16` / `17` / `18` 只作为历史兼容入口保留。
- 实际完成情况、测试记录和复盘以 `15-迭代执行记录.md` 和 `archive/` 为准。

使用方式：

```text
先看 02 判断当前版本
↓
再看 03 确认 MVP 产品范围
↓
再看当前 iteration 文档和其引用的验收依据
↓
最后用本文档拆任务和安排开发顺序
```

## 2. 当前执行版本

当前执行版本：

```text
V4-B research_completed / pending_user_review
```

当前核心目标：

```text
用户确认 V4-B 方案（§11）后更新 07/11/19，再进入代码实现。
```

当前执行要求：

```text
V4-A 已验收：docs/iterations/v4-a-runtime-multimodal-foundation.md
V4-B 任务单：docs/iterations/v4-b-aesthetic-trajectory-temporal-profiling.md
引用：v4-0 §V4-B、docs/19 §10.1、07 §5.14
下一步：用户确认 §11 → 实现 migration + timeline_builder + API + 前端
```

当前不处理：

- V4-B 审美时间轴、V4-C 知识图谱、V4-D Agent/MCP。
- 音乐/视频真实内容解析。
- 生产级 LangSmith / OpenTelemetry。

## 3. 当前技术栈

当前技术栈：

- 前端：React + TypeScript + Vite。
- 后端：FastAPI + Pydantic。
- 业务数据库：PostgreSQL。
- 向量库：ChromaDB；V4-A 已接入 input vector runtime 写入（`CHROMA_ENABLED` 控制）。
- 文件存储：本地文件目录（`POST /api/files/upload`），后续可替换对象存储。
- AI 能力：mock/heuristic 为默认；V4-A 已接入可选 openai/ollama embedding 与图片上传。
- 测试：pytest + 前端构建检查。

历史版本任务拆分不再保存在本文档：

- V1：`archive/v1/V1-任务拆分与里程碑计划.md`。
- V2：`archive/v2/V2-任务拆分与里程碑计划.md`。
- V3：`archive/v3/V3-任务拆分与里程碑计划.md`。

## 4. 历史版本任务拆分：V3（已归档）

V3 任务拆分、子阶段状态与里程碑结果见：

```text
archive/v3/V3-任务拆分与里程碑计划.md
```

V3 归档说明见 `archive/v3/V3-归档说明.md`。

## 5. AI 生成代码顺序

每次让 AI 写代码时，按下面顺序拆小任务：

1. Pydantic schema 和 TypeScript type 对齐。
2. Repository 或当前 in-memory store。
3. Service。
4. Workflow step。
5. API route。
6. 前端 service。
7. 前端页面或 feature component。
8. 单元测试或集成测试。
9. 文档状态更新。

每次生成代码前必须提供：

- 本次任务目标。
- 涉及的权威文档。
- 输入 schema。
- 输出 schema。
- 验收标准。
- 不允许 AI 自行决定的边界。

## 6. 每次开发的约束

- 不直接在 API route 中写业务流程。
- 不在 service 中拼接 prompt。
- 不让 LLM 原始输出直接入库。
- 不把 ChromaDB 当业务数据库。
- 不把相似性分组说成绝对聚类结论。
- 不输出人格诊断、玄学判断或无依据高级词。
- 不把后续版本 runtime 能力提前塞进当前研究或验收阶段。

### 6.1 AI 辅助开发治理约束

本项目允许 AI 辅助生成代码，但不允许用“兜底、兼容、默认值、mock 或跳过逻辑”掩盖错误。

开发期默认规则：

- 核心业务事实失败必须 fail fast，例如输入不存在、报告不存在、feedback 关联不到 insight、schema 校验失败、数据库写入失败。
- 非核心增强能力可以 graceful degradation，但必须记录为显性降级事件，并说明 original error、fallback action、developer message、userVisible 和测试覆盖。
- Mock / heuristic / placeholder / dev-only 行为必须可见，不能在文档、API 或 UI 中暗示为真实 AI 能力。
- 测试失败时先定位根因，不优先修改测试预期；只有当测试不符合权威设计文档时才允许调整测试。
- 一处实现不抽象；只有稳定重复或当前版本明确需要替换实现时，才新增 interface / factory / registry。
- 未来版本能力只能进入文档、边界提示或预留字段，不提前实现未被当前 iteration 验收的 runtime。

### 6.2 Developer Debug Panel 实施边界

Developer Debug Panel 作为 V2 横向基础设施分阶段推进，不阻断 V2-C 主线。

实施顺序：

1. 先记录治理原则和验收标准。
2. 再补 `fallback_events`、`mock_usage`、`schema_validation`、`boundary_warnings` 等最小调试语义。
3. 最后实现最小开发者面板，只展示 workflow trace、fallback events、mock usage、schema validation、boundary warnings。

明确不做：

- 不做复杂 dashboard。
- 不做实时刷新。
- 不接入 Langfuse / LangSmith / OpenTelemetry runtime。
- 不提前实现 RAG、Agent、MCP 或审美时间轴 runtime（V4-B 起单独迭代）。

## 7. 当前最推荐的下一步

当前下一步是 V4-B 方案用户复核：

```text
docs/iterations/v4-b-aesthetic-trajectory-temporal-profiling.md §11
```

执行规则：

- V4-B §5 调研已完成；确认后更新 `07` / `11` / `19` §10.1，再写代码。
- 不提前实现 V4-C / V4-D / V4-E。
- 不引入 LLM 周期摘要或 Agent runtime。
