# AI 审美形成系统：V2 归档说明

## 1. 归档状态

当前归档状态：

```text
归档类型：V2 Memory / User Model 阶段性实现记录
验收状态：V2 未完成 / 未归档
归档日期：暂未归档
```

说明：

本文件当前不是最终归档说明，而是 V2 归档前的阶段性记录。

它只表示当前已经完成并验证了 V2 的一部分能力：历史报告、报告详情回看、轻量画像、profile evidence，以及部分 feedback 保存 / 修改 / 画像修正链路。它不表示 V2 整体已经完成，也不表示 V2 可以归档。

V2 的子阶段过程记录不在本文档展开，详见 `../../iterations/` 和 `../../15-迭代执行记录.md`。

## 2. V2 一句话目标

```text
在 V1 的报告与反馈闭环之上，加入历史报告、轻量画像、可追溯 profile evidence，以及 feedback 对画像的修正能力。
```

V2 的画像定义：

```text
基于用户输入、报告洞察和用户反馈的轻量审美观察记录。
```

## 3. V2 范围来源

V2 范围由以下文档共同确定：

- `../../02-版本迭代路线图.md`：V2 在整体路线中的位置。
- `../../07-数据结构与系统架构文档.md`：V2 Memory / User Model 数据结构和系统边界。
- `../../11-模块拆分与接口测试文档.md`：V2 API、repository、service 和前端模块契约。
- `../../13-验证与评估文档.md`：V2 验证标准和治理检查。
- `../../17-V2开发收口清单.md`：V2 工程验收清单。
- `../../iterations/v2-0-memory-user-model-research.md`：V2 版本级研究和架构拆分。

## 4. 当前观察到的实现快照

已实现前端能力：

- 历史报告页。
- 轻量画像页。
- 报告详情页 feedback 保存和修改入口。
- mock / fallback 报告不写入真实 feedback 的界面保护。
- 开发模式重复提交保护。

已实现后端能力：

- 历史报告列表 API：`GET /api/users/{user_id}/reports`。
- 报告详情 API：`GET /api/reports/{report_id}`。
- 读取当前反馈 API：`GET /api/insights/{insight_id}/feedback`。
- 提交或修改反馈 API：`POST /api/insights/{insight_id}/feedback`。
- 只读画像 API：`GET /api/users/{user_id}/profile`。
- 分析任务 debug API：`GET /api/analysis-jobs/{job_id}/debug`。
- profile builder，可从 reports + feedback 重建轻量画像。

已实现数据库能力：

- V2 复用 V1 的报告、洞察、反馈和日志表。
- V2 新增 `user_profiles`、`profile_items`、`profile_evidence`。
- `aesthetic_reports.report_json` 支持历史报告详情回看。
- `insight_feedback` 支持同一 `user_id + insight_id` 的当前反馈更新语义。

## 5. 当前已知验证结果

已完成的阶段性自动验证：

```text
后端：python -m pytest，当前通过。
前端：npm run build，当前通过。
Migration：Alembic upgrade head，当前通过。
PostgreSQL runtime：报告生成、feedback 保存、profile 查询链路，当前通过。
```

已完成的阶段性人工验证：

```text
历史报告页：可查看报告列表并进入详情。
轻量画像页：可展示 summary、items、evidence refs、feedback evidence。
feedback：真实报告可提交反馈；mock / fallback 报告不写入真实画像。
重复提交：开发模式下同一轮分析不再重复写入 inputs。
```

这些结果只能证明当前已实现部分通过阶段性验证，不能作为 V2 最终归档结论。

## 6. 未完成 / 待最终复核

V2 归档前仍需确认：

- V2 最终完整用户路径复验。
- V2-C feedback 更新、rejected / uncertain 分区和画像更新语义的完整人工验收。
- 生产环境 Developer Debug Panel 不可见性。
- PostgreSQL 中是否存在重复输入、orphan feedback、缺失 evidence 等数据问题。
- `V2-遗留问题.md` 与 `15-迭代执行记录.md` 是否同步最终状态。

后续版本仍需确认：

- 真实图片文件存储。
- 图片内容进入真实多模态分析。
- 真实 LLM / vision / embedding runtime。
- ChromaDB runtime 写入和查询。
- personalized retrieval / RAG。
- Agent / MCP / 知识图谱。
- 用户手动隐藏、删除或编辑 profile item 的完整权限与审计工作流。
- 匿名用户持久 ID 或登录系统的身份边界。
- Pydantic / FastAPI alias warning 是否作为 tech debt 单独清理。

## 7. 当前结论

```text
V2 未完成 / 未归档。
当前文件只记录阶段性实现和阶段性验收结果。
V2 最终是否可归档，应以后续完整收口验收为准。
```
