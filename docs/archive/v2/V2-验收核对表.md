# AI 审美形成系统：V2 验收核对表

## 1. 使用方式

这份表用于最终确认 V2 是否可以正式归档。

状态说明：

```text
[ ] 未实现或未核对
[~] 部分完成或等待人工复核
[x] 已完成并已验收
[-] V2 不适用
```

正式归档前，所有 V2 必做项都应该是 `[x]`，或者在 `V2-遗留问题.md` 中说明为什么可以延后。

当前状态：

```text
V2 accepted / archived
```

当前结论：

```text
V2-A 和 V2-B 已通过验收。
当前 feedback 保存、mock/fallback 防写入、重复输入写入修复已通过局部测试。
V2-D 最近两次报告对比已完成实现、自动验证和前端人工验收。
V2-E 稳定验收与记忆治理检查已完成自动验收。
V2 final closure 人工复核已通过。
V2 archive gate 已完成。
```

规格依据：

- `../../17-V2开发收口清单.md`
- `../../15-迭代执行记录.md`
- `../../13-验证与评估文档.md`
- `../../iterations/v2-0-memory-user-model-research.md`
- `../../iterations/v2-a-report-history.md`
- `../../iterations/v2-b-profile-evidence.md`
- `../../iterations/v2-c-feedback-weight-memory-update.md`
- `../../iterations/v2-d-report-comparison.md`

## 2. 前端页面

### 2.1 已验收能力

- [x] 首页可进入上传流程。
- [x] 首页可进入历史报告页。
- [x] 首页可进入轻量画像页。
- [x] 首页不暗示人格诊断、心理评估、推荐决策或真实 Agent 能力。
- [x] 历史报告页按时间倒序展示报告列表。
- [x] 历史报告页有明确空状态。
- [x] 历史报告可进入报告详情。
- [x] 轻量画像页展示 profile summary。
- [x] 轻量画像页展示 profile items。
- [x] 轻量画像页展示 evidence refs。
- [x] 轻量画像页展示 feedback evidence。
- [x] 画像页文案可读，不直接暴露内部字段作为主文案。
- [x] 页面不输出人格、心理、能力或命运判断。
- [x] 历史报告页可进入最近变化页。
- [x] 最近变化页可展示变化摘要、底层特征变化、解释方向变化和 evidence refs。
- [x] 最近变化页在历史不足两份时展示明确空状态。

### 2.2 已修复但仍属于收口观察项

- [x] 真实后端报告可以提交 insight feedback。
- [x] 真实后端报告可以修改同一 insight 的 feedback。
- [x] 本地 mock / fallback 报告不会写入真实 feedback 或画像。
- [x] 开发模式下同一轮分析不会重复写入输入样本。
- [x] 开发模式下分析页不会卡在“正在保存输入样本”。

### 2.3 收口观察项

- [x] 上传 / 分析页失败态在当前 V2 final closure 路径下可接受，fallback 边界可见。
- [x] rejected / uncertain 条目的视觉分区已通过 final closure 人工复核。
- [x] 生产环境不展示 Developer Debug Panel。
- [x] V2-E 稳定验收与记忆治理自动检查已通过。

## 3. API

### 3.1 已验收能力

- [x] `GET /api/users/{user_id}/reports` 可查询历史报告列表。
- [x] 历史报告列表返回 `ReportHistoryResponse`。
- [x] 历史报告列表包含 `reports`、`total`、`limit`、`offset`。
- [x] 历史报告摘要包含 `reportId`、`jobId`、`title`、`summary`、`inputCount`、`createdAt`。
- [x] 历史报告按创建时间倒序返回。
- [x] 历史报告按 `user_id` 过滤。
- [x] `GET /api/reports/{report_id}` 存在报告时返回 `ReportResponse`。
- [x] `GET /api/reports/{report_id}` 不存在报告时返回 404。
- [x] `GET /api/users/{user_id}/profile` 可查询用户轻量画像。
- [x] profile 有 evidence 时返回 `UserProfile`。
- [x] profile 无足够 evidence 时返回 `profile: null` 和明确 message。
- [x] `GET /api/users/{user_id}/reports/comparison/latest` 可查询最近两次报告对比。
- [x] 历史报告不足两份时 comparison API 返回明确 message，不生成变化。
- [x] comparison API 返回 feature changes、interpretation changes、summary、disclaimer 和 evidence refs。

### 3.2 已修复但仍属于收口观察项

- [x] `GET /api/insights/{insight_id}/feedback` 已反馈时返回当前 feedback。
- [x] `GET /api/insights/{insight_id}/feedback` 未反馈时返回 `null`。
- [x] `POST /api/insights/{insight_id}/feedback` 可提交 feedback。
- [x] `POST /api/insights/{insight_id}/feedback` 可修改同一 insight 的 feedback。
- [x] 不存在的 insight 提交 feedback 时返回 404。
- [x] 非法 rating 提交时返回 422。

### 3.3 收口观察项

- [x] `GET /api/analysis-jobs/{job_id}/debug` 已可用，但只作为开发能力，生产前端不展示。
- [x] V2-E 治理检查相关 profile builder 数据路径已通过定向测试和完整回归。

## 4. 数据与存储

### 4.1 已验收能力

- [x] 历史报告保存到 `aesthetic_reports`。
- [x] `aesthetic_reports.user_id` 可用于历史报告查询。
- [x] `aesthetic_reports.job_id` 可用于关联分析任务。
- [x] `aesthetic_reports.report_json` 可反序列化为 `ReportResponse`。
- [x] report detail 不依赖前端 mock data。
- [x] `user_profiles` 可保存用户画像摘要。
- [x] `profile_items` 可保存画像条目。
- [x] `profile_evidence` 可保存画像证据。
- [x] 每个 profile item 至少有一条 evidence。
- [x] profile evidence 可追溯到 feature / report / interpretation / insight / feedback 中的至少一种来源。
- [x] 最近报告对比复用 `aesthetic_reports.report_json`，不新增持久化表。

### 4.2 已修复但仍属于收口观察项

- [x] insight 保存到 `insights`。
- [x] insight 关联 `report_id`。
- [x] insight 保存 evidence refs。
- [x] 新生成的 mock insight id 带 report 作用域，避免不同报告复用同一 insight id。
- [x] feedback 保存到 `insight_feedback`。
- [x] feedback rating 只允许 `not_me`、`unsure`、`somewhat_me`、`very_me`。
- [x] 同一 `user_id + insight_id` 再次反馈时更新当前 feedback，不重复累计。
- [x] feedback 可作为 profile evidence 来源。
- [x] PostgreSQL 中匿名用户创建具备并发安全处理。

### 4.3 收口观察项

- [x] `not_me` / `unsure` 对 profile summary 和状态语义的自动治理检查已通过。
- [x] V2 归档前已核对 PostgreSQL 中无重复写入、无 orphan feedback、无缺失 evidence。

## 5. Workflow / 服务边界

### 5.1 已验收能力

- [x] API route 只处理 HTTP 层。
- [x] 业务逻辑进入 service。
- [x] 数据访问进入 repository。
- [x] workflow 能从创建分析任务执行到保存报告。
- [x] workflow 生成的 report id 可用于后续查询。
- [x] workflow 生成的 insight id 可用于后续 feedback。
- [x] profile builder 从 reports + feedback 构建画像。
- [x] profile builder 不凭空生成无 evidence 的画像项。
- [x] report comparison service 从最近两份 report_json 生成只读对比结果。
- [x] report comparison service 不在 API route 中写业务流程。
- [x] ChromaDB 不作为 V2 业务记忆来源。

### 5.2 已修复但仍属于收口观察项

- [x] feedback 更新后重新查询 profile，画像可反映当前 feedback。
- [x] 本地 mock / fallback 报告不会写入真实 feedback 或 profile evidence。

### 5.3 收口观察项

- [x] V2-E profile evidence coverage、被否定解释复现和 feedback influence 定向检查已执行。
- [x] V2 最终归档前后端测试、前端 build 和 PostgreSQL runtime 人工复核已通过。

## 6. 边界与表达

### 6.1 已确认边界

- [x] 不包含完整登录系统。
- [x] 不包含真实图片文件存储。
- [x] 不包含真实图片内容读取。
- [x] 不包含真实 LLM / vision / embedding runtime。
- [x] 不包含 ChromaDB runtime 写入和查询。
- [x] 不包含 personalized retrieval。
- [x] 不包含 RAG。
- [x] 不包含 Agent。
- [x] 不包含 MCP。
- [x] 不包含知识图谱。
- [x] 不包含音乐 / 视频输入。
- [x] 不包含自动推荐。
- [x] 不把画像用于消费引导或自动化重大决策。
- [x] 页面不做人格诊断。
- [x] 页面不做心理评估。
- [x] 页面不使用玄学表达。

### 6.2 收口观察边界

- [x] 不把单次输入固化为长期偏好，当前 V2 通过 recent / stable / evidence 边界控制。
- [x] 不把被否定解释作为正向记忆，已通过 V2-E 自动治理检查和 final closure 人工复核。
- [x] 不把不确定反馈强化为正向画像，已通过 V2-E 自动治理检查和 final closure 人工复核。
- [x] 页面不使用无 evidence 的高级词作为画像结论，已通过 final closure 人工复核。
- [x] 最近变化页面不把两次输入写成人格、心理或能力变化。

## 7. 已有测试记录

后端：

```text
2026-06-17：
命令：REPOSITORY_BACKEND=memory python -m pytest
结果：30 passed, 3 warnings
覆盖：
- V1 API flow
- V2 历史报告查询
- V2 profile repository / builder
- feedback 保存和修改
- profile evidence 方向映射
- report-scoped mock insight id
- database repository 持久化链路
- 最近两次报告对比 API 和 builder
- 历史不足两份时 comparison empty state
- V2-E memory governance validation

说明：这是 V2 归档验收自动测试记录。
```

前端：

```text
2026-06-17：
命令：npm run build
结果：通过
说明：这是 V2 归档验收前端构建记录。
```

人工验收：

```text
2026-06-17：
- 历史报告列表与详情回看通过。
- 轻量画像页 summary / items / evidence refs / feedback evidence 展示通过。
- 真实报告 feedback 提交和修改通过。
- fallback / mock 报告不写入真实 feedback，通过。
- 开发模式同一轮分析不重复写入 inputs，通过。
- V2-D 最近报告对比路径通过。
- React StrictMode 下分析页卡在“正在保存输入样本”的问题已修复并通过。
- V2 final closure 完整路径人工复核通过。
- PostgreSQL runtime 表数据复核通过：无重复 inputs、无 orphan feedback、profile evidence 可追溯。
- 页面整体不输出人格、心理或能力诊断式表达，通过。

说明：人工复核已通过，archive gate 已完成。
```

## 8. 遗留问题

非 blocking / 可后续处理：

- Pydantic / FastAPI alias warning 仍存在。
- 当前 AI 能力仍为 mock / heuristic 边界，不代表真实模型效果。
- 图片输入仍未接入真实文件存储和真实图片内容读取。

当前阻塞 V2 归档的事项：

```text
当前没有 blocking issue。
V2 已完成 archive gate。
```

## 9. 归档判断

当前判断：

```text
V2 accepted / archived。
当前文档记录 V2 最终验收结果。
```
