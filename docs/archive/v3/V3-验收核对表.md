# AI 审美形成系统：V3 验收核对表

## 1. 使用方式

状态说明：

```text
[ ] 未实现或未核对
[~] 部分完成或等待人工复核
[x] 已完成并已验收
[-] V3 不适用
```

当前状态：

```text
V3 accepted / archived
```

规格依据：

- `V3-开发收口清单.md`
- `../../15-迭代执行记录.md`
- `../../13-验证与评估文档.md`
- `../../iterations/v3-0-personalized-retrieval-research.md`
- `../../iterations/v3-a-personalized-history-retrieval.md`
- `../../iterations/v3-b-aesthetic-knowledge-rag.md`
- `../../iterations/v3-c-evaluation-metrics-baseline.md`
- `../../iterations/v3-d-retrieval-rag-observability.md`
- `../../iterations/v3-e-stability-governance-validation.md`

## 2. 后端 / Workflow

- [x] workflow 包含 `retrieve_personal_history`。
- [x] workflow 包含 `retrieve_aesthetic_knowledge`。
- [x] workflow 包含 `compute_report_evaluation`。
- [x] `historyContext` 写入 `report_json`。
- [x] `knowledgeContext` 写入 `report_json`。
- [x] `evaluationMetrics` 写入 `report_json`。
- [x] insight `evidenceRefs` 只绑定当前 input ids。
- [x] history / knowledge context 与 insight evidence 分离。
- [x] knowledge 不进入 profile positive evidence。
- [x] `GET /api/reports/{report_id}/evaluation` 可用。
- [x] `GET /api/analysis-jobs/{job_id}/debug` 返回 V3-D trace 字段。

## 3. 前端

- [x] 报告详情展示「历史参考」。
- [x] 报告详情展示「知识参考」。
- [x] 报告详情展示「质量评估」。
- [x] feedback 后 evaluation 自动刷新。
- [x] dev 环境 Developer Debug 展示 retrieval / context / evaluation trace。
- [x] 页面不输出人格、心理、能力或命运判断。

## 4. 治理与指标

- [x] 第一份报告 history abstain。
- [x] 第二份报告 history items 带 matchedFeatures / sourceRefs。
- [x] knowledge items 带 sourceRefs / matchedFeatures。
- [x] `not_me` feedback → feedbackHitRate 0%（设计预期）。
- [x] `not_me` 历史反馈 direction 为 negative。
- [x] workflow 正常路径 unsupportedInsightCount = 0。
- [x] `test_v3_governance_validation.py` 全部通过。

## 5. 测试

- [x] memory backend：54 passed。
- [x] database backend：integration + repo tests 6 passed（需清空业务数据）。
- [x] frontend build 通过。

## 6. 归档结论

```text
V3 必做项均为 [x]。
V3 accepted / archived。
```
