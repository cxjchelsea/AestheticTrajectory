# AI 审美形成系统：V4 开发收口清单

## 1. 归档状态

```text
归档类型：V4 engineering closure checklist snapshot
状态：accepted / archived
归档日期：2026-06-22
来源：docs/21-V4开发收口清单.md
```

## 2. V4 收口范围

V4 收口时验收的核心范围：

- V4-0 版本级研究与子阶段拆分（V4-A → V4-E）。
- V4-A runtime / 存储 / ChromaDB / 多模态 schema。
- V4-B 审美时间轴与 temporal profiling。
- V4-C 可追溯知识图谱 + 可选向量 rerank。
- V4-D evidence-bound observation Agent + MCP internal tools + external import 治理。
- V4-E grouping stability、failure replay、V4 横切治理验收。
- evidence-first：history / knowledge / graph / agent / external 不进入 profile positive evidence。
- mock / dev-only 边界在 debug 中可见。

## 3. V4 必做范围（子阶段 checkbox）

### 3.1 V4-0 版本级研究与架构拆分

- [x] 完成 `v4-0-long-term-personalized-agent-research.md`。
- [x] 明确 V4 子阶段：V4-A → V4-B → V4-C → V4-D → V4-E。
- [x] 创建 `docs/19-记忆与用户模型设计文档.md` 作为 Memory / User Model 语义权威。

### 3.2 V4-A Runtime & Multimodal Foundation

- [x] embedding factory（mock / openai / ollama）。
- [x] Chroma input vector upsert（`CHROMA_ENABLED` 控制）。
- [x] 文件上传 API + 图片 `fileUrl`。
- [x] music / video metadata-only 输入 schema。
- [x] 人工验收通过。

### 3.3 V4-B Aesthetic Trajectory & Temporal Profiling

- [x] migration `20260618_0003`：`aesthetic_timeline_events`。
- [x] workflow `update_trajectory` + timeline API + TimelinePage。
- [x] 周期摘要与 weakening 展示。
- [x] 人工验收通过。

### 3.4 V4-C Knowledge Graph & External RAG Runtime

- [x] migration `20260618_0004`：concepts + relations seed。
- [x] 1-hop graph 扩展 + knowledge API。
- [x] 可选 knowledge Chroma rerank。
- [x] 图谱 / RAG 不进 profile positive evidence。
- [x] 人工验收通过。

### 3.5 V4-D Agent Runtime & MCP Integration

- [x] migration `20260618_0005`：observation / agent_action / external_import。
- [x] `AgentToolRegistry` + observation API + ProfilePage 观察摘要。
- [x] external import confirm/reject 治理流。
- [x] 可选 `backend/mcp/internal_tools_server.py` stdio MCP。
- [x] 人工验收通过。

### 3.6 V4-E Evaluation Maturity & Governance Validation

- [x] `GroupingStabilityService` + API。
- [x] `FailureReplayService` + API。
- [x] Debug：`groupingStabilityTrace`、`failureReplay`。
- [x] `test_v4e_governance_validation.py`。
- [x] 人工验收通过。

### 3.7 V4 final closure

- [x] legacy issue audit 完成（见 `V4-遗留问题.md`）。
- [x] 归档文档齐全（`archive/v4/`）。
- [x] `docs/12`、`docs/15`、`docs/README.md`、`21-V4开发收口清单.md` 已更新。

## 4. V4 明确不做

- [x] 不接入生产级 LangSmith / OpenTelemetry pipeline。
- [x] 不做 LLM-as-judge evaluation dashboard。
- [x] 不做无边界自主 Agent 或静默 external write。
- [x] 不把 supplementary context 写入 profile positive evidence。

## 5. 归档判断

```text
V4 所有计划子阶段均已完成验收。
无 blocking 遗留问题。
V4 accepted / archived。
```
