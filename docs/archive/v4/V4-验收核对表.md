# AI 审美形成系统：V4 验收核对表

## 1. 使用方式

状态说明：

```text
[x] 已完成并已验收
[-] V4 不适用
```

当前状态：

```text
V4 accepted / archived
```

当前结论：

```text
V4-A 至 V4-E 均已完成实现、自动验证和人工验收。
V4 final closure / archive gate 已完成。
```

规格依据：

- `V4-开发收口清单.md`
- `../../15-迭代执行记录.md` §20–25
- `../../13-验证与评估文档.md` §15.1.6–15.1.7
- `../../iterations/v4-*`

## 2. V4 版本级验收（v4-0 §12）

- [x] 新模态输入能进入统一表征链路，且来源可追溯。
- [x] 审美时间轴能展示历史变化，条目可展开 evidence。
- [x] 知识图谱与外部 RAG 只增强解释，不进入 profile positive evidence。
- [x] Agent 每个行为有 reason、tool trace、evidence refs，可审计。
- [x] MCP 导入经用户确认，外部内容标记为 supplementary context。
- [x] 真实 runtime 与 mock 边界在 debug/trace 中可见。
- [x] grouping stability 与 failure replay 具备 baseline 实现与测试。
- [x] 页面与 Agent 输出不做人格诊断、心理评估、命运判断或审美规训。
- [x] V4 结束前有 archive gate 和 legacy issue audit。

## 3. 前端

### 3.1 V4-A 多模态

- [x] 图片上传后样本含 `fileUrl`。
- [x] music / video 样本为 metadata-only 占位文案。

### 3.2 V4-B 时间轴

- [x] TimelinePage 展示 events 与周期摘要。
- [x] 事件可关联 report / feedback evidence。

### 3.3 V4-C 知识参考

- [x] 报告详情「知识参考」含关系说明（graph 扩展）。
- [x] 知识 / 图谱不进入 profile 正向证据。

### 3.4 V4-D Agent

- [x] ProfilePage 可触发「生成观察摘要」。
- [x] 观察摘要含 evidenceRefs；无 report 时 abstain。
- [x] external import 需 confirm 后才可用。
- [x] Agent 摘要不进入 profile positive evidence。

### 3.5 V4-E Debug

- [x] Debug Panel 展示 Grouping Stability（score / disclaimer）。
- [x] Debug Panel 展示 Failure Replay 步骤链。
- [x] boundary_warnings 中 Agent/MCP 为 `dev_only`，非 `planned`。

### 3.6 V2/V3 基线未回退

- [x] 分析 → 报告 → feedback → profile → comparison 主路径仍可用。

## 4. API

### 4.1 V4 新增 / 扩展

- [x] `POST /api/files/upload`
- [x] `GET /api/users/{user_id}/timeline`（及 summary）
- [x] `GET /api/aesthetic-knowledge/concepts|graph|chunks`
- [x] `POST/GET /api/users/{user_id}/observations`
- [x] `GET /api/users/{user_id}/agent-actions`
- [x] `POST/GET /api/users/{user_id}/external-imports` + confirm/reject
- [x] `GET /api/reports/{report_id}/grouping-stability`
- [x] `GET /api/analysis-jobs/{job_id}/failure-replay`
- [x] debug API 含 `groupingStabilityTrace`、`failureReplay`

## 5. 自动测试

- [x] `REPOSITORY_BACKEND=memory`，`pytest app/tests -q` → **96 passed**（2026-06-22）。
- [x] `test_v3_governance_validation.py` 仍通过。
- [x] `test_v4d_governance.py` 通过。
- [x] `test_v4e_governance_validation.py` 通过。

## 6. 治理

- [x] insight evidenceRefs 仍只引用当前 report inputs。
- [x] history / knowledge / graph / agent / external 不 feed profile positive evidence。
- [x] supplementary context 与 preference fact 边界在测试中有覆盖。

## 7. 明确不做（归档时确认）

- [x] 未接入 LangSmith / OpenTelemetry 生产 pipeline。
- [x] 未做 LLM-as-judge evaluation dashboard。
- [x] 未做无边界自主 Agent。

## 8. 归档判断

```text
V4 必做项均为 [x]。
遗留项已在 V4-遗留问题.md 重分类为 carry_over，无 blocking。
V4 accepted / archived。
```
