# AI 审美形成系统：V4 遗留问题

## 1. 文档职责

这份文档记录 V4 收口阶段的遗留问题及其后续处理状态。

状态说明：

```text
resolved：已经在 V4 当前子阶段中解决
pending_validation：功能已实现或局部测试通过，但仍等待最终人工验收或全路径复核
carry_over：尚未解决，明确延后到指定版本或任务
blocking：阻塞 V4 归档，必须先解决
wont_fix：确认不处理，并说明原因
```

## 2. 当前结论

```text
V4 已归档。
V4-0 至 V4-E 均已完成验收。
V4 final closure / archive gate 已完成。
无 blocking / pending_validation 遗留问题。
```

## 3. 已解决（含自 V1/V2/V3 carry_over 转入 V4 的项）

| 状态 | 问题 | 解决版本 | 当前结论 |
| --- | --- | --- | --- |
| `resolved` | ChromaDB runtime 写入和查询 | V4-A / V4-C | input + knowledge collection；`CHROMA_ENABLED` 控制 |
| `resolved` | 可选真实 embedding runtime（ollama/openai） | V4-A | factory + 启动说明 §6 |
| `resolved` | 真实图片文件存储与读取 | V4-A | `POST /api/files/upload` + `fileUrl` |
| `resolved` | 审美时间轴 | V4-B | timeline events + API + UI |
| `resolved` | 知识图谱与外部 RAG runtime 增强 | V4-C | 1-hop graph + optional vector rerank |
| `resolved` | Agent / MCP baseline | V4-D | observation agent + stdio MCP + external import 治理 |
| `resolved` | grouping stability 评估指标 | V4-E | pairwise co-membership + API |
| `resolved` | failure replay 记录与回放 | V4-E | logs + fallback 只读视图 + API |
| `resolved` | V3 carry_over：music/video 输入面 | V4-A | metadata-only schema |
| `resolved` | V3 carry_over：Agent、MCP、知识图谱、时间轴 | V4-B/C/D | 按子阶段落地 |

## 4. 归档阻塞检查

```text
当前没有 V4 blocking / pending_validation 遗留问题。
```

## 5. 延后到后续版本

| 状态 | 问题 | 计划解决版本 / 任务 | 当前结论 |
| --- | --- | --- | --- |
| `carry_over` | LangSmith / OpenTelemetry 生产级 pipeline | V5+ / observability | V4-E 仅 boundary warning |
| `carry_over` | 复杂 evaluation dashboard / LLM-as-judge | V5+ | V4-E baseline 指标已足够 |
| `carry_over` | 真实 vision / LLM report runtime | V5+ | workflow 仍为 mock/heuristic |
| `carry_over` | 音乐、视频真实内容解析 | V5+ / MCP | metadata-only 占位 |
| `carry_over` | 生产 OAuth MCP + 外部 SaaS 收藏源 | V5+ | V4-D mock import batch 已验证治理 |
| `carry_over` | failure replay 一键重跑 workflow | V5+ | V4-E 只读回放 |
| `carry_over` | cross-user / 长期 grouping stability 平台 | V5+ | V4-E pairwise baseline |
| `carry_over` | 报告详情 API 对象级 user scope | security / identity | 匿名用户体系下未完整实现 |
| `carry_over` | 登录系统或持久匿名 ID | identity / V5+ | Agent/MCP 前需方案，V4-D 用 mock 流验证 |
| `carry_over` | integration test 依赖干净 DB（database 模式） | tech-debt | memory 模式跑全量 pytest |
| `carry_over` | Pydantic / FastAPI alias warnings 清理 | tech-debt | 不阻塞功能 |
| `carry_over` | `CHROMA_ENABLED=true` 但 Chroma 不可达时 knowledge 步骤 500 | tech-debt | write_vectors 可降级；knowledge init 待与 write_vectors 对齐 |
| `carry_over` | V5 级多 Agent 协作 | V5+ | 明确不做 |

## 6. Wont Fix

```text
当前没有 V4 wont_fix 遗留问题。
```

## 7. 归档判断

```text
V4 遗留问题已完成重分类。
V1/V2/V3 中指向 V4 的 carry_over 项已在 V4 解决或重新分配到 V5+ / tech-debt。
V4 accepted / archived。
```
