# AI 审美形成系统：V6 任务拆分与里程碑计划

## 1. 归档状态

```text
归档类型：V6 task breakdown snapshot
状态：accepted / archived
归档日期：2026-06-23
```

本文件记录 V6 归档时的版本任务拆分快照。过程记录仍以 `../../iterations/` 和 `../../15-迭代执行记录.md` 为准。

## 2. V6 版本主题

```text
Multimodal Runtime & Production Hardening
```

V6 的执行顺序来自 `../../iterations/v6-0-multimodal-runtime-production-hardening-research.md`：

```text
V6-0 -> V6-A -> V6-B -> V6-C -> V6-D -> V6-E -> V6 final closure / archive
```

## 3. 子阶段任务拆分

### 3.1 V6-0：版本级调研与架构拆分

- [x] 明确 V6 major 主题。
- [x] 确认 Agent frontier 方向：Multimodal Preference Modeling、Evaluation / Observability、Governance。
- [x] 复核 V5 遗留项并重分类。
- [x] 确认子阶段顺序：Image → Audio/Music → Video → Hardening → Governance。
- [x] 明确 V6 不做企业级多模态平台、复杂 LLM-as-judge dashboard、自动下载流媒体内容。

### 3.2 V6-A：Image Understanding Runtime

- [x] 新增 image feature extractor adapter。
- [x] 支持 `mock`、`disabled`、`ollama_vision` runtime。
- [x] 输出结构化 `InputFeature`，包含 evidence、promptVersion、modelName。
- [x] image governance validator 禁止诊断/身份/能力类判断。
- [x] debug 中可见 image mock/real/disabled 边界。
- [x] 自动验证通过。
- [ ] 真实 vision 模型人工验收后置。

### 3.3 V6-B：Audio / Music Understanding Boundary

- [x] 新增 audio/music feature extractor adapter。
- [x] 支持 `metadata_only`、`text_notes`、`mock`、`disabled` runtime。
- [x] metadata-only 不声称听见鼓点、旋律、音色、节拍等真实音频内容。
- [x] text-notes 仅使用用户主动提供的 lyrics/transcript/notes。
- [x] debug 中可见 music runtime 边界。
- [x] 自动验证通过。

### 3.4 V6-C：Video Understanding Boundary

- [x] 新增 video feature extractor adapter。
- [x] 支持 `metadata_only`、`text_notes`、`mock`、`disabled` runtime。
- [x] metadata-only 不声称看见画面、镜头、剪辑、运动、光线等真实视频内容。
- [x] text-notes 仅使用用户主动提供的 subtitle/description/shot notes。
- [x] debug 中可见 video runtime 边界。
- [x] 自动验证通过。

### 3.5 V6-D：Production Hardening & Runtime CI

- [x] 精确过滤 Pydantic/FastAPI alias compatibility warning，保留 API JSON 契约。
- [x] pytest 默认 upload 目录隔离到临时目录。
- [x] `.env.example` 与 settings 默认 runtime 保持一致。
- [x] 默认 pytest baseline 不依赖 Postgres / Chroma / Ollama / 真实多模态模型。
- [x] debug boundary 对 image/music/video runtime 状态有测试覆盖。
- [x] 自动验证通过。

### 3.6 V6-E：Multimodal Governance Validation & Closure Prep

- [x] 新增 V6-E 横切 integration tests。
- [x] 验证 report `evidenceRefs` 只引用当前 inputs。
- [x] 验证 profile feature-only evidence 不能在无 feedback 时成为 `stable`。
- [x] 验证 cross-user report/job/debug/profile/file scope。
- [x] 验证 disabled runtime fail-fast，不 silent fallback mock。
- [x] 验证 report insight 不输出人格、心理、能力、命运、身份推断类表达。
- [x] 整理 V6 final closure prep legacy issue 初稿。
- [x] 自动验证通过。

## 4. 验证基线

```text
python -m pytest backend/app/tests -q
151 passed
```

## 5. 下一阶段入口

```text
V6 accepted / archived。
下一步如继续推进，应先创建 V7-0 版本级调研与架构闸门，不直接进入功能实现。
```
