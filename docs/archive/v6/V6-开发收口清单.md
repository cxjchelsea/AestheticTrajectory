# AI 审美形成系统：V6 开发收口清单

## 1. 归档状态

```text
归档类型：V6 engineering closure checklist snapshot
状态：accepted / archived
归档日期：2026-06-23
来源：docs/23-V6开发收口清单.md
```

## 2. V6 收口范围

V6 收口时验收的核心范围：

- V6-0 版本级研究与子阶段拆分（V6-A → V6-E）。
- V6-A image understanding runtime adapter 与 mock/disabled/ollama_vision 边界。
- V6-B audio/music metadata-only 与 text-notes 边界。
- V6-C video metadata-only 与 text-notes 边界。
- V6-D production hardening / runtime CI baseline。
- V6-E 横切 governance validation 与 closure prep。
- V1-V5 evidence-first、profile governance、identity scope、runtime visibility 不变量继续成立。

## 3. V6 必做范围（子阶段 checkbox）

### 3.1 V6-0 版本级研究与架构拆分

- [x] 完成 `v6-0-multimodal-runtime-production-hardening-research.md`。
- [x] 明确 V6 子阶段：V6-A → V6-B → V6-C → V6-D → V6-E。
- [x] 明确 V6 不做企业级多模态平台、复杂 LLM-as-judge dashboard、自动下载流媒体内容。

### 3.2 V6-A Image Understanding Runtime

- [x] 引入 `ImageFeatureExtractor` adapter。
- [x] 支持 `IMAGE_FEATURE_RUNTIME=mock|disabled|ollama_vision`。
- [x] mock image feature 标记为 dev-only placeholder。
- [x] disabled image runtime fail-fast，不 silent fallback mock。
- [x] ollama_vision 输出必须是可校验 JSON，并转为 `InputFeature`。
- [x] image feature governance validator 阻止诊断/身份/能力类判断。
- [x] debug mock usage / boundary warning 可见 image runtime 状态。
- [x] 自动验证通过。
- [ ] 真实 vision 模型人工验收后置。

### 3.3 V6-B Audio / Music Understanding Boundary

- [x] 引入 `AudioMusicFeatureExtractor` adapter。
- [x] 支持 `MUSIC_FEATURE_RUNTIME=metadata_only|text_notes|mock|disabled`。
- [x] metadata-only 不声称真实音频内容解析。
- [x] text-notes 只使用用户主动提供的歌词、转写或备注。
- [x] disabled music runtime fail-fast，不 silent fallback mock。
- [x] music feature governance validator 阻止诊断/能力/命运类判断。
- [x] debug mock usage / boundary warning 可见 music runtime 状态。
- [x] 自动验证通过。

### 3.4 V6-C Video Understanding Boundary

- [x] 引入 `VideoFeatureExtractor` adapter。
- [x] 支持 `VIDEO_FEATURE_RUNTIME=metadata_only|text_notes|mock|disabled`。
- [x] metadata-only 不声称真实视频内容解析。
- [x] text-notes 只使用用户主动提供的字幕、描述或分镜备注。
- [x] disabled video runtime fail-fast，不 silent fallback mock。
- [x] video feature governance validator 阻止身份/诊断/能力/命运类判断。
- [x] debug mock usage / boundary warning 可见 video runtime 状态。
- [x] 自动验证通过。

### 3.5 V6-D Production Hardening & Runtime CI

- [x] 默认 pytest 使用 memory backend + mock/metadata-only runtime。
- [x] 默认测试不依赖 Postgres / Chroma / Ollama / 真实多模态模型。
- [x] upload API 测试写入 pytest 临时 `UPLOAD_DIR`，不污染项目根目录。
- [x] Pydantic/FastAPI alias compatibility warning 使用精确 pytest filter 管理。
- [x] `.env.example` 与 `settings` 默认 runtime 值保持一致。
- [x] Debug boundary 对 image/music/video runtime 状态保持自动测试覆盖。
- [x] 自动验证通过。

### 3.6 V6-E Multimodal Governance Validation & Closure Prep

- [x] 新增 `test_v6e_multimodal_governance_validation.py`。
- [x] 覆盖 runtime boundary、evidenceRefs、profile governance、identity scope、disabled fail-fast。
- [x] 修复 feature-only profile item 在无 feedback 时不可成为 `stable`。
- [x] 后端全量 pytest 151 passed。
- [x] closure prep legacy issue 初稿已整理。

### 3.7 V6 final closure

- [x] legacy issue audit 完成（见 `V6-遗留问题.md`）。
- [x] 归档文档齐全（`archive/v6/`）。
- [x] `docs/12`、`docs/15`、`docs/README.md`、`23-V6开发收口清单.md` 已更新。

## 4. V6 明确不做

- [x] 不做企业级多模态平台。
- [x] 不做自动下载流媒体内容。
- [x] 不做复杂 LLM-as-judge evaluation dashboard。
- [x] 不把 metadata-only / placeholder 伪装成真实媒体内容解析。
- [x] 不把多模态 feature-only evidence 直接写成 `stable` profile preference。
- [x] 不把真实 runtime 失败 silent fallback 到 mock。
- [x] 不在 V6 强制接入生产级 LangSmith / OpenTelemetry / Sentry。

## 5. 归档判断

```text
V6 所有计划子阶段均已完成验收。
无 blocking 遗留问题。
V6 accepted / archived。
```
