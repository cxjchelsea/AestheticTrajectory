# AI 审美形成系统：V5 验收核对表

## 1. 使用方式

状态说明：

```text
[x] 已完成并已验收
[-] V5 不适用
```

当前状态：

```text
V5 accepted / archived
```

当前结论：

```text
V5-A 至 V5-E 均已完成实现、自动验证和人工验收。
V5 final closure / archive gate 已完成。
```

规格依据：

- `V5-开发收口清单.md`
- `../../15-迭代执行记录.md` §26-§31
- `../../13-验证与评估文档.md` §15.3 / §15.4
- `../../iterations/v5-*`

## 2. V5 版本级验收（v5-0 §10）

- [x] 真实 LLM 路径可生成报告，且 insight `evidenceRefs` 仍只指向当前 inputs。
- [x] 用户身份持久化；跨 user 不可读取他人 report/profile/debug/external import。
- [x] 至少 1 个 OAuth/mock_oauth 只读外部源经用户 confirm 后进入 external context。
- [x] Chroma / knowledge vector 不可达时核心 workflow 不 500；Level 0 仍 fail fast。
- [x] V4 治理测试 + V5 新增治理测试全部通过。
- [x] mock / real runtime 边界在 debug 中可见。
- [x] 页面与 LLM 输出不做人格诊断、心理评估、命运判断或审美规训。
- [x] V5 结束前有 archive gate 和 legacy issue audit。

## 3. 前端

### 3.1 Identity / Session

- [x] anonymous session 可 bootstrap。
- [x] 刷新后同一用户可继续访问自己的报告。
- [x] 第二个 session/user 不能访问第一个用户的资源。

### 3.2 Report Runtime

- [x] `REPORT_LLM_RUNTIME=ollama` 时报告可生成。
- [x] insights 展示 evidence refs / input id。
- [x] Debug 中 mock / real runtime 边界可见。

### 3.3 External Source

- [x] Profile 页可连接外部上下文源。
- [x] preview 后为 `pending_confirmation`。
- [x] confirm 后才变为 `confirmed`。
- [x] confirmed external context 不直接变成 profile positive evidence。

### 3.4 Resilience / Debug

- [x] Chroma 不可达时报告仍可完成。
- [x] Developer Debug 可见 Chroma / knowledge vector 降级信息。

## 4. API

### 4.1 V5 新增 / 扩展

- [x] `POST /api/session/bootstrap`
- [x] `GET /api/session/me`
- [x] `POST /api/users/{user_id}/external-sources/{provider}/connect`
- [x] `GET /api/users/{user_id}/external-sources/{provider}/oauth/callback`
- [x] `POST /api/users/{user_id}/external-sources/{provider}/imports/preview`
- [x] object-level scope 覆盖 report/job/debug/profile/external import。
- [x] debug API 含 `authContext`、`mockUsage`、`boundaryWarnings`、`fallbackEvents`、`retrievalTrace`。

## 5. 自动测试

- [x] `python -m pytest backend/app/tests/integration/test_v5e_governance_validation.py -q` → **3 passed, 3 warnings**。
- [x] `python -m pytest backend/app/tests/integration/test_v5a_identity.py backend/app/tests/unit/test_v5b_governance_validation.py backend/app/tests/integration/test_v5c_external_sources.py backend/app/tests/unit/test_v5d_resilience.py backend/app/tests/integration/test_v5e_governance_validation.py -q` → **18 passed, 3 warnings**。
- [x] `python -m pytest backend/app/tests -q` → **120 passed, 5 warnings**。
- [-] Frontend 未在 V5-E 改动，未运行 build；V5-A/B/C 改动阶段已分别构建或人工验收。

## 6. 治理

- [x] insight evidenceRefs 仍只引用当前 report inputs。
- [x] history / knowledge / graph / agent / external 不 feed profile positive evidence。
- [x] supplementary context 与 preference fact 边界在测试中有覆盖。
- [x] real LLM 失败不 silent fallback mock。
- [x] database / user scope / schema validation 仍 fail-fast。

## 7. 明确不做（归档时确认）

- [x] 未接入生产级 LangSmith / OpenTelemetry pipeline。
- [x] 未做 LLM-as-judge evaluation dashboard。
- [x] 未做企业 IAM / SSO。
- [x] 未做 V5 multi-agent 平台。

## 8. 归档判断

```text
V5 必做项均为 [x]。
遗留项已在 V5-遗留问题.md 重分类，无 blocking。
V5 accepted / archived。
```
