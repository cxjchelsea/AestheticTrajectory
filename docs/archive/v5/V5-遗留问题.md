# AI 审美形成系统：V5 遗留问题

## 1. 文档职责

这份文档记录 V5 收口阶段的遗留问题及其后续处理状态。

状态说明：

```text
resolved：已经在 V5 当前子阶段中解决
pending_validation：功能已实现或局部测试通过，但仍等待最终人工验收或全路径复核
carry_over：尚未解决，明确延后到指定版本或任务
blocking：阻塞 V5 归档，必须先解决
wont_fix：确认不处理，并说明原因
```

## 2. 当前结论

```text
V5 已归档。
V5-0 至 V5-E 均已完成验收。
V5 final closure / archive gate 已完成。
无 blocking / pending_validation 遗留问题。
```

## 3. 已解决（含自 V4 carry_over 转入 V5 的项）

| 状态 | 问题 | 解决版本 | 当前结论 |
| --- | --- | --- | --- |
| `resolved` | 登录系统或持久匿名 ID | V5-A | anonymous session + `user_sessions` |
| `resolved` | 报告 / profile / debug / external import 对象级 user scope | V5-A / V5-E | cross-user tests 覆盖 403 |
| `resolved` | 真实 LLM report runtime | V5-B | Ollama InterpretationGenerator + validator |
| `resolved` | LLM 输出 evidenceRefs / governance | V5-B / V5-E | evidenceRefs 指向当前 inputs；禁诊断治理 |
| `resolved` | 生产 OAuth MCP / 外部只读源最小路径 | V5-C | mock_oauth connect/callback/preview/confirm |
| `resolved` | external import token redaction 与 user scope | V5-C / V5-E | token 不出前端；scope tests 覆盖 |
| `resolved` | `CHROMA_ENABLED=true` 但 Chroma 不可达时 knowledge 步骤 500 | V5-D | knowledge vector graceful degrade |
| `resolved` | V4 carry_over：V5 横切治理验收 | V5-E | `test_v5e_governance_validation.py` |

## 4. 归档阻塞检查

```text
当前没有 V5 blocking / pending_validation 遗留问题。
```

## 5. 延后到后续版本

| 状态 | 问题 | 计划解决版本 / 任务 | 当前结论 |
| --- | --- | --- | --- |
| `carry_over` | 生产级 LangSmith / OpenTelemetry pipeline | V6+ / observability | V5-D/V5-E 仅记录边界；非 V5 硬门槛 |
| `carry_over` | 复杂 evaluation dashboard / LLM-as-judge | V6+ | V5-E 使用治理测试和人工验收，不做 judge dashboard |
| `carry_over` | 音乐、视频真实内容解析 | V6+ / multimodal runtime | V5 仍 metadata-only |
| `carry_over` | 真实 vision 内容分析 | V6+ / multimodal runtime | V5-B 先 text LLM report |
| `carry_over` | 生产级 OAuth provider（非 mock_oauth） | V6+ / external integrations | V5-C 完成可替换 OAuth 边界与 mock_oauth 验证 |
| `carry_over` | integration test 干净 DB / testcontainers | V6+ / CI hardening | V5-D 记录 memory CI + 本机 DB smoke 策略 |
| `carry_over` | Pydantic / FastAPI alias warnings 清理 | V6+ / framework tech-debt | 5 条 warning 已定位为兼容噪声，不阻塞 V5 |
| `carry_over` | failure replay 一键重跑 workflow | V6+ | V4-E 只读回放仍为当前边界 |
| `carry_over` | cross-user / 长期 grouping stability 平台 | V6+ | V4-E pairwise baseline 仍足够 |
| `wont_fix` | V5 级 multi-agent 协作平台 | 不做于 V5 | V5-0 明确排除，避免偏离 trust boundary |

## 6. Wont Fix

```text
V5 内不做 multi-agent 协作平台、企业 IAM、复杂 evaluation SaaS dashboard。
这些不是 V5 Production Runtime & Trust Boundary 的收口目标。
```

## 7. 归档判断

```text
V5 遗留问题已完成重分类。
V4 carry_over 中指向 V5 的必做项已在 V5 解决。
剩余项已明确分配到 V6+ / tech-debt / wont_fix。
V5 accepted / archived。
```
